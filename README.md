
## ⚠️ heads up

this is just a test script I made for messing around with automation

## what it does

- opens google chrome
- waits until chrome is active
- then repeats this every 5 seconds:
  - presses `H`
  - presses `W`
  - presses `9`

it only works when chrome is the active window
